---
name: greeter
description: Greet a user by name. Use when the user asks for a greeting or says hello.
---

To greet a user, run:

```bash
node /opt/skills/greeter/greeter.cjs <name>
```

Replace `<name>` with the person's name. Returns a friendly greeting message.
