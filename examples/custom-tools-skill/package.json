{
  "name": "@sandbox-agent/example-custom-tools-skill",
  "private": true,
  "type": "module",
  "scripts": {
    "build:script": "tsup src/list-files.ts --format cjs --platform node --target node18 --minify --out-dir dist",
    "start": "tsx src/upload-skill.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sandbox-agent/example-shared": "workspace:*",
    "sandbox-agent": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsup": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
